export interface News {
    id: string;
    title: string;
    body: string;
    image_url: string;
    author: string;
    date: Date;
    active: boolean;
    createdAt: Date;
    updatedAt: Date;
} 